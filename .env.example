# =============================================================================
# Mistorneos - Variables de entorno
# Copiar a .env y ajustar valores. En producci?n no subas .env al repositorio.
# =============================================================================

# Entorno: development | production (auto-detecta por host si no se define)
APP_ENV=development

# URL base de la aplicaci?n (opcional)
# - Si no se define: en localhost se usa autom?ticamente http://localhost/mistorneos_fvd
# - En producci?n FVD: https://laestaciondeldominohoy.com/mistorneos_fvd
#   Ver plantilla completa: .env.production.example
# - Entorno de pruebas (subpath): https://laestaciondeldominohoy.com/pruebas
# APP_URL=

# Debug (solo desarrollo)
APP_DEBUG=false

# -----------------------------------------------------------------------------
# Panel integral FVD (FvdAdminGate)
# -----------------------------------------------------------------------------
# false (default en producción): solo torneo_gestion + operaciones de mesa.
# true: reactiva usuarios, clubes, finanzas, afiliación, ranking admin,
#       inscripción pública en línea, auditoría, comentarios portal, etc.
FVD_ADMIN_ENABLED=true

# Panel institucional FVD (default true): sin mesas/rondas/resultados en esta app.
# false = reactivar administración operativa de torneos en la misma instalación.
FVD_INSTITUTIONAL_ONLY=true

# -----------------------------------------------------------------------------
# Base de datos principal (mistorneos_fvd)
# -----------------------------------------------------------------------------
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=mistorneos_fvd
DB_USERNAME=root
DB_PASSWORD=
DB_CHARSET=utf8mb4

# -----------------------------------------------------------------------------
# Base de datos secundaria (personas / fvdadmin) - opcional
# -----------------------------------------------------------------------------
# DB_SECONDARY_HOST=localhost
# DB_SECONDARY_PORT=3306
# DB_SECONDARY_DATABASE=personas
# DB_SECONDARY_USERNAME=root
# DB_SECONDARY_PASSWORD=

# -----------------------------------------------------------------------------
# Seguridad
# -----------------------------------------------------------------------------
# Nombre de la cookie de sesi?n
SESSION_NAME=mistorneos_session

# Duraci?n de sesi?n (evita "sesi?n expirada" con el panel abierto o importaci?n masiva).
# SESSION_GC_MAXLIFETIME=28800   # segundos que el servidor conserva la sesi?n (ej. 8 h)
# SESSION_COOKIE_LIFETIME=28800 # vida de la cookie en el navegador (segundos)
# SESSION_LIFETIME=240          # opcional: minutos (si no define SESSION_GC_MAXLIFETIME, se usa max(esto?60, ...))

# Dominio de la cookie de sesion (opcional). Si entran por www y sin www, use el punto inicial:
# SESSION_COOKIE_DOMAIN=.laestaciondeldominohoy.com
# Regenerar ID de sesion cada N segundos (anti session fixation). Por defecto en codigo = 0 (desactivado).
# Solo activar si lo necesita su politica de seguridad: SESSION_REGENERATE_AFTER_SECONDS=28800
# SESSION_REGENERATE_AFTER_SECONDS=0

# Debug de sesi?n: escribe [SESSION_DEBUG] en error_log en cada paso (login, index, profile, etc.).
# Poner SESSION_DEBUG=1 para diagnosticar p?rdida de sesi?n. En session_start_early solo aplica si est? en el servidor (SetEnv).
# SESSION_DEBUG=0

# Clave CSRF (generar una aleatoria de 32+ caracteres en producci?n)
CSRF_KEY=replace_with_random_32_chars

# -----------------------------------------------------------------------------
# Producci?n (opcional)
# -----------------------------------------------------------------------------
# Forzar redirecci?n HTTPS en producci?n (true|false)
# FORCE_HTTPS=true

# -----------------------------------------------------------------------------
# Analitica web — Umami Cloud
# -----------------------------------------------------------------------------
# UMAMI_API_KEY=tu_clave_api_umami
# URL iframe panel admin — opción A (share):
# UMAMI_SHARE_URL=https://cloud.umami.is/share/tu_token/mistorneos_fvd
# URL iframe panel admin — opción B (dashboard regional Umami Cloud):
# UMAMI_SCRIPT_URL=https://cloud.umami.is/analytics/US/websites/TU_WEBSITE_ID/mistorneos_fvd
# Tracker público (solo si difiere del default):
# UMAMI_SCRIPT_URL=https://cloud.umami.is/script.js
# UMAMI_WEBSITE_ID=1e64a2c9-7c79-49f0-b6a2-6d8c761893d3

# Cron consolidación analítica local (public/modules/cron_analytics.php)
# ANALYTICS_CRON_KEY=una_clave_secreta_larga
